<Window x:Class="SylverInk.XAML.Update"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:SylverInk"
		xmlns:vm="clr-namespace:SylverInk.XAML.ViewModels"
		mc:Ignorable="d"
		d:DataContext="{d:DesignInstance Type=vm:UpdateViewModel}"
		Background="{Binding AppSettings.MenuBackground}" Foreground="{Binding AppSettings.MenuForeground}" MouseLeftButtonDown="Drag" Title="Sylver Ink: Update" Height="150" Width="450">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<ProgressBar Grid.ColumnSpan="3" Height="30" Minimum="0" Maximum="1" Value="{Binding Progress}" Width="375" x:Name="UpdateProgress" />

		<Button Command="{Binding CancelCommand}" Grid.Column="2" Content="Cancel" Grid.Row="1" />
	</Grid>
</Window>
